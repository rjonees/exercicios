#Algoritmo que receba dois números, multiplique e mostre o resultado
n1 = float(input('Digite um numero'))
n2 = float(input('Digite outro numero'))
multiplicação = n1 * n2
print('O resultado da operação é:', multiplicação)

#ALgoritmo qu receba 5 numeros e faça media aritmetica
n1 = float(input('Digite um numero'))
n2 = float(input('Digite outro numero'))
n3 = float(input('Digite outro numero'))
n4 = float(input('Digite outro numero'))
n5 = float(input('Digite outro numero'))
media = (n1+n2+n3+n4+n5)/5
print('A medica aritmetica dos numeros digitados é:', media)

#Algoritmo que receba o valor de um produto e calcule o acrescimo de 8%
valor = float(input('Digite o valor do produto'))
novo = valor+(valor*8/100)
print('O valor do produto com acrésccimo de 8% é R$:', novo)

#Algoritmo que receba dois numeros e apresente o resultado da subtração entre eles
n1 = float(input('Digite um numero'))
n2 = float(input('Digite outro numero'))
subtração = n1 - n2
print('O resultado da operação é:', subtração)

#Algoritmo que receba altura e peso, mostre o resultado do IMC
altura = bool(input('Digite sua altura, ex: 1.70'))
peso = float(input('Digite seu peso'))
IMC = peso / altura ** 2
print('O resultado do seu IMC é:', IMC)

#Algoritmo de conversão de Celsius para Fahrenheit
celcius = float(input('Digite a temperatura em graus'))
fah = (celcius * 9 / 5) + 32
print('A temperatura convertida é:', fah)

#Algoritmo que receba as horas trabalhadas e o valor da hora, tambem aclcule o salario total
horas = float(input('Digite a quantidade de horas trabalhadas'))
valor = float(input('Digite o valor da hora trabahada'))
salario = horas * valor
print('O salário total é R$', salario)
